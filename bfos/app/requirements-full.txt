# BRANDFORGE OS - requirements (full) — Python 3.10+
# Optional integrations: real-Chrome browser audit and Telegram gateway.
# Heavy — install only when you want these features.

-r requirements.txt

# SQLite keyword memory is the supported default. ChromaDB is NOT installed
# by this extra: unresolved upstream advisories require a separate review.
# Do not expose a Chroma service alongside this local app.

# Real browser audit (needs: playwright install chromium)
playwright>=1.44.0

# Telegram phone control (async)
python-telegram-bot>=20.7
httpx>=0.27.0
