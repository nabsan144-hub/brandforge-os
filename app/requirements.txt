# BRANDFORGE OS - requirements (core)
# Install: pip install -r requirements.txt
# Full features: pip install -r requirements-full.txt  (vector memory, real browser, Telegram)

# Core — required
rich==13.7.1
requests==2.34.2
python-dotenv==1.2.3
fastapi==0.141.1
uvicorn==0.52.4
websockets==16.0
pydantic>=2.7.4,<3.0
python-multipart==0.0.32

# Scheduling (daemon heartbeat)
apscheduler==3.10.4

# Native client-ready exports
fpdf2==2.8.8
uharfbuzz==0.56.1
python-docx==1.2.0
Pillow==12.3.0

# Raster export of SVG deliverables (PNG/JPEG for ad platforms) — wheel only, no system deps
resvg-py==0.5.0

# Test client transport (starlette>=1.6 TestClient requires httpx2 on fresh installs)
httpx2==2.12.0
