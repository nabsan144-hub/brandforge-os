# Local 1.11.0 evidence

Final release-gates.txt is authoritative for full-suite counts. Other logs are focused or browser runs, not additive unique test counts. No external service or paid provider certification is implied. Native smoke is Linux only and uses the explicit partial-core export. Private-version and layout logs predate only metrics/docs changes. Older failure/retry logs are not represented as passing.
