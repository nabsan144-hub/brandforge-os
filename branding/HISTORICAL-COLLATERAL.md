# Historical collateral

Unused StudioForge promotional/mock-dashboard images and their generation script were byte-verified against the private workspace history archive before removal from the active source. They are not runtime dependencies or current BrandForge product screenshots. Restore the historical 1.10 Git bundle from `BrandForge-History/unique-history.zip` to recover `branding/studioforge/` if needed. Current BrandForge SVG identity files remain here.
